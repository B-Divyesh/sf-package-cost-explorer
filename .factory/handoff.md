# Package Cost Explorer — independent QA handoff

## Outcome: FAIL

Candidate `2f6622bf946a3cb948a5454430b6d83f5c143438` was independently verified
from a clean checkout on 2026-08-27 against
<https://package-cost-explorer.sociobot.in>. The live static frontend exactly
matches the candidate assets and the core browser analysis passes, but the
required production badge endpoint fails: the live UI-generated `/badge.svg`
URL returns **HTTP 500 `Backend call failure`**, not SVG. This P1 deployment
defect blocks acceptance.

Detailed commands, product coverage, response-policy evidence, budgets, and
the exact reproduction URL are in `.factory/verification-3.md`.

## What passed

- clean root and API installs; 23 unit/product tests;
- desktop + 390px Playwright/axe checks (4 passed, 2 intentional skips);
- production build/type check; clean PWA update and offline regression;
- production normal, malformed, missing-package recovery, and high-scale
  `date-fns@4.1.0` analysis (741 entry rows, 250 named exports in 185 s);
- live frontend candidate-asset hash comparison, privacy/outbound-request
  checks, service-worker offline smoke, and response-header/cache inspection.

`npm run test:live` is currently expected to fail until the production badge
worker is repaired. After repair run:

```sh
npm ci
npm ci --prefix api
npm test
npm run test:e2e
npm run test:pwa-update
npm run build
npm run test:live
```

## Required next step

Fix/enable the deployed Azure attached API rewrite `/badge.svg` → `/api/badge`
so the exact badge URL generated by a live report returns HTTP 200,
`image/svg+xml`, and safe SVG content. Then commission a fresh verification.
