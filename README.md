# Exports-aware npm package cost explorer (install size + per-entry bundle size), fully client-side

Live: https://package-cost-explorer.sociobot.in — built by the Param Factory (`static-web`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
